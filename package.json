{
  "name": "aevo-pos",
  "private": true,
  "packageManager": "bun@1.2.21",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:api": "bun run --filter @aevo/api dev",
    "dev:web": "bun run --filter @aevo/web dev",
    "build": "bun run --filter '*' build",
    "deploy:web": "bun run --filter @aevo/web deploy",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test packages apps",
    "test:integration": "TEST_DATABASE_URL=${TEST_DATABASE_URL:-postgres://aevo:aevo@localhost:5433/aevo_test} bun test packages/db/test",
    "db:migrate": "bun run --filter @aevo/db migrate",
    "db:seed": "bun run --filter @aevo/db seed"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "typescript": "^5.9.2"
  }
}
